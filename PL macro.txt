How to install and run it
Open the Visual Basic Editor: Press ALT + F11 
on your keyboard.

Insert a Module: Go to Insert > Module.


Paste the Code: Copy the code above and paste it into the white window.

Run the Macro:

Press F5 while inside the code, OR 

Go back to your Excel sheet, press ALT + F8, select IncrementUntilMatch, and click Run.


Sub MarketLoop()
    Dim ws As Worksheet
    Dim stepSize As Double
    Set ws = ThisWorkbook.ActiveSheet
    
    ' Set initial value of D6 starting from H10
    ws.Range("D6").Value = ws.Range("H10").Value
    
    ' Scenario 1: L (Incrementing)
    If ws.Range("C2").Value = "L" Then
        Do Until ws.Range("G14").Value >= ws.Range("H10").Value
            ' --- New Step Size Condition ---
            If ws.Range("D6").Value < 100 Then
                stepSize = 0.1
            Else
                stepSize = 1
            End If
            ' -------------------------------
            
            ws.Range("D6").Value = ws.Range("D6").Value + stepSize
            DoEvents
            
            ' Safety Exit (adjusted for potential high iterations)
            If ws.Range("D6").Value > ws.Range("H10").Value + 500000 Then Exit Do
        Loop

    ' Scenario 2: P (Decrementing)
    ElseIf ws.Range("C2").Value = "P" Then
        Do Until ws.Range("G14").Value <= ws.Range("H10").Value
            ' --- New Step Size Condition ---
            If ws.Range("D6").Value < 100 Then
                stepSize = 0.1
            Else
                stepSize = 1
            End If
            ' -------------------------------
            
            ws.Range("D6").Value = ws.Range("D6").Value - stepSize
            DoEvents
            
            ' Safety Exit
            If ws.Range("D6").Value < ws.Range("H10").Value - 500000 Then Exit Do
        Loop
    End If
    
    MsgBox "Loop Complete for " & ws.Range("C2").Value
End Sub


